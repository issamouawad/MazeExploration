

MAS miners {
    infrastructure: Centralised

    environment: MazeEnv.MazePlanet(1,200,yes)
        // parameters: 1. environment configuration id (from 1 to 3)
        //             2. sleep time (in ms) after each action
        //             3. whether display the gui

    agents:
        //leader;
        mazerunner #4;
}
